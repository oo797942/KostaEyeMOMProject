<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="admin">

<!-- 게임등록 sql -->
<insert id="gameInput" parameterType="gameVO"> <!-- insert,delete,update는 리턴타입 필요없음 -->
INSERT INTO kid_game(g_no, g_title, g_content, g_photo, g_flash) VALUES (MOM_GAME_NO.nextval, #{g_title}, #{g_content}, #{g_photoName}, #{g_flashName})
</insert>
<!-- userId 같은 이름들은 parameterClass의 클래스 필드 명 -->
 
</mapper> 